<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fast.system.mapper.UserMapper">

    <resultMap type="User" id="UserResult">
        <id     property="userId"       column="user_id"      />
        <result property="userName"     column="user_name"    />
        <result property="password"     column="password"     />
        <result property="sex"          column="sex"          />
        <result property="role"         column="role"         />
        <result property="createTime"   column="create_time"  />
        <result property="updateBy"     column="update_by"    />
        <result property="updateTime"   column="update_time"  />
    </resultMap>

    <select id="selectUserByUserName" parameterType="String" resultMap="UserResult">
        select user_id, user_name, password, sex, role, create_time, update_by, update_time
        from user
        where user_name = #{userName}
    </select>

    <select id="selectUserList" parameterType="User" resultMap="UserResult">
        select user_id, user_name, sex, role, create_time, update_by, update_time
        from user
        <where>
            <if test="userName != null and userName != ''">
                AND user_name like concat('%', #{userName}, '%')
            </if>
        </where>
    </select>

    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="userId">
        insert into user (
            user_name,
            password,
            sex,
            role,
            create_time
        ) values (
                     #{userName},
                     #{password},
                     #{sex},
                     'common',   sysdate()
                 )
    </insert>

    <update id="updateUser" parameterType="User">
        update user
        <set>
            <if test="userName != null and userName != ''">user_name = #{userName},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="role != null and role != ''">role = #{role},</if>

            update_by = #{updateBy},
            update_time = sysdate()
        </set>
        where user_id = #{userId}
    </update>

    <delete id="deleteUserById" parameterType="Long">
        delete from user where user_id = #{userId}
    </delete>

</mapper>